import 'package:flutter/material.dart';

class FudgeView extends StatelessWidget {
  static const String route = "/fudge";

  const FudgeView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Fudge");
  }
}
