import 'package:flutter/material.dart';

class OldTimerView extends StatelessWidget {
  static const String route = "oldTimer";

  const OldTimerView({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text("Old Timer");
  }
}
